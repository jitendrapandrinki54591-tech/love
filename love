<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Happy Valentine's Day ❤️</title>
  <style>
    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4, #fbc2eb);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .card {
      background: white;
      max-width: 900px;
      width: 90%;
      border-radius: 24px;
      box-shadow: 0 20px 40px rgba(0,0,0,0.2);
      overflow: hidden;
    }

    .header {
      background: linear-gradient(135deg, #ff4d6d, #ff758f);
      color: white;
      padding: 40px 20px;
      text-align: center;
    }

    .header h1 {
      margin: 0;
      font-size: 2.5rem;
    }

    .header p {
      margin-top: 10px;
      font-size: 1.1rem;
    }

    .content {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 30px;
      padding: 40px;
    }

    .message {
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .message h2 {
      color: #ff4d6d;
      margin-bottom: 10px;
    }

    .message p {
      font-size: 1.05rem;
      line-height: 1.6;
      color: #444;
    }

    .photo-slot {
      border: 3px dashed #ff758f;
      border-radius: 20px;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 20px;
      background: #fff5f7;
    }

    .photo-slot p {
      color: #ff4d6d;
      font-weight: 600;
    }

    .hearts {
      text-align: center;
      padding-bottom: 30px;
      font-size: 1.8rem;
    }

    @media (max-width: 768px) {
      .content {
        grid-template-columns: 1fr;
      }
    }
    @keyframes floatHearts {
      0% { transform: translateY(0) scale(1); opacity: 1; }
      100% { transform: translateY(-100vh) scale(1.5); opacity: 0; }
    }

    .floating-hearts span {
      position: fixed;
      bottom: -50px;
      font-size: 24px;
      animation: floatHearts linear infinite;
      color: #ff4d6d;
      pointer-events: none;
    }
      .gallery {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 15px;
    }

    .gallery img {
      width: 100%;
      height: 220px;
      object-fit: cover;
      border-radius: 18px;
      box-shadow: 0 10px 20px rgba(0,0,0,0.15);
      transition: transform 0.3s ease;
    }

    .gallery img:hover {
      transform: scale(1.05);
    }

    #loveOverlay{
      position:fixed;
      inset:0;
      background:linear-gradient(135deg,#ff9a9e,#fad0c4,#fbc2eb);
      display:flex;
      flex-direction:column;
      align-items:center;
      justify-content:center;
      z-index:9999;
      color:#fff;
      text-align:center;
      font-weight:600;
    }
    .love-btn{
      font-size:64px;
      cursor:pointer;
      animation:pulse 1.4s infinite;
      filter:drop-shadow(0 10px 25px rgba(0,0,0,.25));
    }
    @keyframes pulse{
      0%{transform:scale(1)}
      50%{transform:scale(1.2)}
      100%{transform:scale(1)}
    }
    .hidden{opacity:0; pointer-events:none;}
</style>
</head>
<body>
  <div id="loveOverlay">
    <div class="love-btn">❤️</div>
    <p>Tap the heart</p>
  </div>
  <div class="floating-hearts">
    <span style="left:10%; animation-duration: 6s;">❤️</span>
    <span style="left:25%; animation-duration: 8s;">💖</span>
    <span style="left:40%; animation-duration: 7s;">💕</span>
    <span style="left:55%; animation-duration: 9s;">💘</span>
    <span style="left:70%; animation-duration: 6.5s;">💗</span>
    <span style="left:85%; animation-duration: 8.5s;">❤️</span>
  </div>
  <div class="card">
    <audio id="bgMusic" loop playsinline>
      <source src="romantic-music.mp3" type="audio/mpeg">
      Your browser does not support the audio element.
    </audio>

    <script>
  const overlay = document.getElementById('loveOverlay');
  const music = document.getElementById('bgMusic');
  document.querySelector('.love-btn').addEventListener('click', ()=>{
    music.play().catch(()=>{});
    overlay.classList.add('hidden');
    setTimeout(()=> overlay.remove(), 600);
  });
</script>

<div class="header">
      <h1>Happy Valentine's Day 💖</h1>
      <p>For my Valentine, <strong>Raaji</strong> 💖</p>
    </div>

    <div class="content">
      <div class="message">
        <h2>My Special Message</h2>
        <p>
          On this Valentine’s Day, I just want you to know how special you are.
          Your smile, your kindness, and the way you make everything better mean
          more than words can say. 💕
        </p>
        <p>
          Thank you for being you. Today is all about celebrating us and the
          beautiful moments we share. 🌹
        </p>
      </div>

      <div class="gallery">
        <img src="photo12.jpeg" alt="Memory 1" />
        <img src="photo1.jpeg" alt="Memory 2" />
        <img src="photo3.jpeg" alt="Memory 3" />
        <img src="photo4.jpeg" alt="Memory 4" />
      </div>
    </div>

    <div class="hearts">❤️ 💕 💖 💗 💘</div>
  </div>
</body>
</html>
